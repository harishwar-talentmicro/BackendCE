angular.module('ezeidApp').controller('ProgressOverlayCtrl',[
    '$rootScope',
    '$scope',
    '$http',
    '$q',
    '$timeout',
    'Notification',
    '$filter',
    '$window',
    'GURL',
    '$interval',
    'MsgDelay',
    '$location',
    '$routeParams',
    function (
        $rootScope,
        $scope,
        $http,
        $q,
        $timeout,
        Notification,
        $filter,
        $window,
        GURL,
        $interval,
        MsgDelay,
        $location,
        $routeParams
        ) {

//        $rootScope.$on('$includeContentRequested',function(){
//            if(!angular.element('progress-overlay').hasClass('hidden')){
//                angular.element('progress-overlay').addClass('hidden');
//            }
//        });
//
//        $rootScope.$on('$includeContentRequested',function(){
//            if(angular.element('progress-overlay').hasClass('hidden')){
//                angular.element('progress-overlay').removeClass('hidden');
//            }
//        });

    }]);